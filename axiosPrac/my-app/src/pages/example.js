//Rotas api para que o app envie requests para.

//example.js 
import { connectToDatabase } from "../../src/utils/mongodb";

export default async (req, res) => {
    if (req.method == "POST") { //o req armazenada os dados enviados na request de post
                                    //endpoint

    }
};
